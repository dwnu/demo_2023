abstract class AppConsts {
  // It's better idea to pass it differently, for example via run configurations or flavors.
  // That way it would be easier to support multiple environments.
  static const baseUrl = 'https://dawidwnukowski.com/api/';
}
